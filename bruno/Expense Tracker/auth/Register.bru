meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{URL}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test1@example.com",
    "password": "example123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
